#include <stdio.h>
#include <stdlib.h>
#include "utils.h"

int binarySearch(unsigned int*, int, unsigned int);

int main(int argc, char const *argv[])
{
    
    return 0;
}

int binarySearch(unsigned int* arr, int size, unsigned int query)
{
    unsigned int left = 0, right = size - 1;
}
